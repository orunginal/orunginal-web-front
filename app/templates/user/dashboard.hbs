<h1>Votre historique de parcours [dashboard.hbs]</h1>

<ul>
{{#each model as |user|}}
	<li>
		<div>{{user.email}}</div>
		<div>{{user.token}}</div>
		<div>{{user.id}}</div>
		<div>{{user.is_admin}}</div>
	</li>
{{/each}}
</ul>

{{outlet}}

