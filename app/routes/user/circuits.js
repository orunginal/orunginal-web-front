import Ember from 'ember';

export default Ember.Route.extend({
	// session: Ember.inject.service(),

	// model() {
	// 	//var path = 'users/'+this.get('session.id')+'/circuits';
	// 	//return this.store.findRecord('users');
	// },
	// afterModel(model) {
	// 	console.log(model.get('length'));
	// }
});
